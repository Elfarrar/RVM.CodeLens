services:
  rvmcodelens-api:
    build:
      context: .
      dockerfile: src/RVM.CodeLens.Web/Dockerfile
    container_name: rvmcodelens-api
    restart: unless-stopped
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080
      - ConnectionStrings__DefaultConnection=Host=rvmtech-db;Database=rvmcodelens;Username=rvmtech;Password=${POSTGRES_PASSWORD}
      - DataProtection__Directory=/app/data/dataprotection
    volumes:
      - app-data:/app/data
    networks:
      - rvmtech

volumes:
  app-data:

networks:
  rvmtech:
    external: true
